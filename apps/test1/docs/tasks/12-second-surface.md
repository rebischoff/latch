# 12 — Second business Surface

> **Status:** Planning stub — not scheduled. Expand when task **11** is complete.

## Goal

Repeat the task **10** loop without hand-holding: add `project` or `task` Surface (pick one), split page, YAML policies, nav entry.

## Delivers

- Second Surface module + DAL + page
- Nav shows both routes for admin role; restricted role differs
- Optional: extract shared split-view component if duplication hurts

## Reference

- Task **10** files as template
- CRM jobs split: [`apps/crm/src/app/(app)/jobs/page.tsx`](../../../crm/src/app/(app)/jobs/page.tsx)

## Prerequisites

- Task **11** complete.
