import { definePolicyRegistry } from "@latch/policy";

/**
 * Business policy registry — empty until Surface YAML is codegen'd.
 * After codegen, import generated `*SurfacePolicyDef` entries here.
 */
export const widgetsRegistry = definePolicyRegistry();
