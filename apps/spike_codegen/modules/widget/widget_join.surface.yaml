# Multi-table surface — glue generation skipped (escape hatch).
id: widget_join
displayName: Widget with tags
anchorTable: widgets

tables:
  - widgets
  - widget_tags

fields:
  - id: summary
    columns:
      - column: widgets.id
        type: string
      - column: widgets.label
        type: string
  - id: tags
    columns:
      - column: widget_tags.tag
        type: string
