# Synthetic vocabulary fixture — policy catalog only (no backing table).
id: alpha_list
displayName: Alpha list
anchorTable: fixture_alpha
kind: business

fieldActions:
  - read
  - write

surfaceActions:
  - read
  - write

tables:
  - fixture_alpha

fields:
  - id: title
    columns:
      - column: fixture_alpha.title
        type: string
  - id: status
    columns:
      - column: fixture_alpha.status
        type: string
  - id: owner
    columns:
      - column: fixture_alpha.owner
        type: string
  - id: category
    columns:
      - column: fixture_alpha.category
        type: string
