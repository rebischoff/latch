CRM_DEV_PASSWORD=demo

# Optional — Neon direct connection (same as apps/web) when testing latch_audit writes
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
