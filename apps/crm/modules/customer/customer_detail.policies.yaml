# customer_detail — role → Field grants (Phase 02)
# @see docs/phases/02-ui-sync/tasks/07-policies-yaml.md
# @see docs/phases/02-ui-sync/decisions.md — admin-only; field_tech has no binding
#
# Runtime truth: apps/crm/src/lib/policy/customer-detail.ts (hand-synced until codegen loads policies).

surface: customer_detail
forbiddenFieldResponse: 404

roles:
  office_admin:
    rowScope: all
    surfaceActions:
      - read
      - write
    fields:
      - field: profile
        actions: [read, write]
      - field: billing
        actions: [read, write]
      - field: sites
        actions: [read, write]
      - field: job_history
        actions: [read]
