# user_roles_detail — IAM Surface (Phase 03, threat T8)
# IAM surface: excluded from data_master wildcard list (task 08).
# @see docs/phases/03-identity-iam/tasks/06-surface-yaml.md

id: user_roles_detail
displayName: User roles
anchorTable: latch_users

tables:
  - latch_users
  - latch_user_roles

fields:
  - id: profile
    columns:
      - latch_users.id
      - latch_users.display_name

  # Logical projection: role_id rows for anchor user → string[] in DAL
  - id: role_assignments
    columns: []
