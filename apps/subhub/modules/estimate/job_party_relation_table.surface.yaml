# Engagement stakeholder relation catalog (editable table page)
id: job_party_relation_table
displayName: Job party relations
anchorTable: job_party_relation

fieldActions:
  - read
  - write

surfaceActions:
  - read
  - write
  - delete

tables:
  - job_party_relation

fields:
  - id: display_name
    columns:
      - column: job_party_relation.id
        type: string
      - column: job_party_relation.display_name
        type: string
  - id: sort_order
    columns:
      - column: job_party_relation.sort_order
        type: number
