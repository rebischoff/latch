# Manufacturers — party subset filtered by party_role.manufacturer (DAL)
id: manufacturer_list
displayName: Manufacturer list
anchorTable: party

fieldActions:
  - read
  - write

surfaceActions:
  - read

tables:
  - party
  - party_role

fields:
  - id: summary
    columns:
      - column: party.id
        type: string
      - column: party.display_name
        type: string
      - column: party.kind
        type: string
