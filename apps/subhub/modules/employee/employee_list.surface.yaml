# Employees — party subset filtered by party_role.employee (DAL)
id: employee_list
displayName: Employee list
anchorTable: party

fieldActions:
  - read
  - write

surfaceActions:
  - read
  - create

tables:
  - party
  - party_role
  - party_person
  - employee

fields:
  - id: summary
    columns:
      - column: party.id
        type: string
      - column: party.display_name
        type: string
      - column: party_person.latch_user_id
        type: string
        nullable: true
