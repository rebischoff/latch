-- Business tables placeholder — replace or extend when adding domain Surfaces.
-- Codegen cross-checks Surface YAML columns against migrations/*.sql in this app.

BEGIN;

-- Example (commented): jobs/customers/sites DDL for trades-CRM surfaces.

COMMIT;
