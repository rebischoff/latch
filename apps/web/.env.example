# Copy to .env.local (not committed)
# cp .env.example .env.local

# Neon: Dashboard → Connect → Direct connection (for migrate + psql)
# Vercel: use Pooled connection string instead
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Stub principal (Step 3 pilot — see docs/foundations/development.md)
LATCH_STUB_USER=seed-field-tech
LATCH_STUB_ROLE=field_tech
