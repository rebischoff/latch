# STATUS — Phase 02 UI sync

> Phase-local quarterback. Global pointer: [`../../../STATUS.md`](../../../STATUS.md).
> Updated: 2026-05-29.

- **Home packages:** `@latch/react`, `apps/web`
- **State:** partial — `<Can>` / `<FieldControl>` exist from the pilot; `customer_detail` not built.

## Right now — do this next

Not the active phase. When picked up: lock the `customer_detail` Surface sketch, then build it reusing `@latch/react` components; add nav-minimal + per-role snapshot tests.

## Blockers

None (uses stub principal until Phase 03).

## Recently completed

- `CapabilitiesProvider`, `<Can>`, `<FieldControl>` delivered during the `job_detail` pilot.
