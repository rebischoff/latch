id: widget_detail
displayName: Widget detail
anchorTable: widgets
kind: business

fieldActions:
  - read
  - write

surfaceActions:
  - read
  - write

tables:
  - widgets

fields:
  - id: label
    columns:
      - column: widgets.label
        type: string
  - id: status
    columns:
      - column: widgets.status
        type: string
  - id: branch_scope
    columns:
      - column: widgets.scope_id
        type: string
